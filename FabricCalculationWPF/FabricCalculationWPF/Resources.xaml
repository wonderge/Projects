<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:FabricCalculationWPF"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <sys:Double x:Key="button_height">34</sys:Double>
    <sys:Double x:Key="button_width">75</sys:Double>
    <sys:Double x:Key="font_size">14</sys:Double>

    <Style TargetType="Button">
        <Setter Property="Width" Value="{StaticResource button_width}"/>
        <Setter Property="Height" Value="{StaticResource button_height}"/>
        <Setter Property="FontSize" Value="{StaticResource font_size}"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource font_size}"/>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="FontSize" Value="{StaticResource font_size}"/>
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="FontSize" Value="{StaticResource font_size}"/>
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style TargetType="StackPanel">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
</ResourceDictionary>